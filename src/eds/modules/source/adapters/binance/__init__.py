"""Источник сделок Binance: USDⓈ-M Futures напрямую с биржи.

Четыре файла с теми же ролями, что у TMM, плюс два своих:

- `rest.py`     — подписанный HTTP-клиент, два базовых адреса, вес запросов
- `ws.py`       — поток пользовательских событий: listenKey и WebSocket
- `mapping.py`  — перевод ответов Binance в наши формы
- `aggregate.py`— сборка сделок из исполнений, чистая функция
- `source.py`   — порт `TradeSource` поверх всего перечисленного

Своих файлов два, потому что у Binance нет готовых сделок: биржа отдаёт
исполнения, а сервис работает со сделками. Агрегатор — единственное место,
где из одного получается другое, и он вынесен отдельно ровно поэтому.
"""
