"""Модули сервиса. Каждый — свой пакет, своя схема в базе.

Правило границ: модуль не импортирует другой модуль. Общее — только
eds.contracts (события и порты) и eds.platform (база, шина, конфиг).
Проверяется тестом tests/test_boundaries.py.
"""
